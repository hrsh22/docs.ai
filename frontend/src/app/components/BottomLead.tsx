"use client"
import React from "react";

const BottomLead = ({

}) => {

    return (
        <div className="flex flex-col justify-center h-screen bg-hero lg:pt-24 lg:pb-14 lg:bg-cover lg:h-full lg:bg-action">
            
        </div>
    );
};

export default BottomLead;
